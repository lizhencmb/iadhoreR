% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_input.R
\name{parse_blast}
\alias{parse_blast}
\title{Parse a BLAST tabular result file for use with i-ADHoRe}
\usage{
parse_blast(blast_file, output_file, evalue_cutoff = 1e-05, min_pident = 0)
}
\arguments{
\item{blast_file}{Path to the BLAST tabular output file (outfmt 6)}

\item{output_file}{Path to write the filtered gene-pair table}

\item{evalue_cutoff}{Maximum e-value to retain a hit. Default: 1e-5}

\item{min_pident}{Minimum percent identity (0-100) to retain a hit.
Default: 0 (no filter)}
}
\value{
Invisibly returns a data frame with columns `query` and `subject`
}
\description{
Reads a BLAST outfmt 6 file, removes self-hits, applies an e-value cutoff,
and writes a two-column tab-separated file of homologous gene pairs suitable
for the `blast_table` setting in i-ADHoRe.
}
\details{
Note: i-ADHoRe automatically adds the reciprocal pair (B, A) for every
pair (A, B) in the table, so duplicate/reciprocal hits do not need to be
kept explicitly.
}
\examples{
\dontrun{
blast_file <- "vvi.blast"  # output from run_diamond()
parse_blast(blast_file, output_file = "blast_table.txt", evalue_cutoff = 1e-5)
}
}
