% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcl.R
\name{blast_to_families}
\alias{blast_to_families}
\title{Cluster BLAST/DIAMOND results into gene families using MCL}
\usage{
blast_to_families(
  blast_file,
  output_file,
  gene_list_files = NULL,
  work_dir = NULL,
  inflation = 3,
  evalue_cutoff = 1e-05,
  threads = 1,
  verbose = FALSE
)
}
\arguments{
\item{blast_file}{Path to BLAST/DIAMOND tabular output (outfmt 6)}

\item{output_file}{Path to write the gene-to-family table}

\item{gene_list_files}{Character vector of gene list file paths (as returned
by [parse_gff()]). When provided, any gene present in the gene lists but
absent from the BLAST results is added as a singleton family. Required
for `table_type=family` in i-ADHoRe. Default: NULL}

\item{work_dir}{Directory for intermediate MCL files. Defaults to the same
directory as `output_file`}

\item{inflation}{MCL inflation parameter (cluster granularity). Higher =
smaller, tighter clusters. Suggested: 1.5-5.0. Default: 3.0}

\item{evalue_cutoff}{Maximum e-value to include a hit. Default: 1e-5}

\item{threads}{Number of threads for MCL. Default: 1}

\item{verbose}{Print MCL tool output. Default: FALSE}
}
\value{
Invisibly returns a data frame with columns `gene` and `family`
}
\description{
Replicates the `mclblastline` pipeline using the individual MCL tools:
`mcxload` to build the graph, `mcl` to cluster, `mcxdump` to export.
Writes a two-column gene-to-family table ready for i-ADHoRe
(`table_type=family`).
}
\examples{
\dontrun{
# Single species
blast_file <- "vvi.blast"  # output from run_diamond()
families <- blast_to_families(blast_file, output_file = "families.txt",
                              gene_list_files = unname(vvi_gene_lists))

# Multiple species: combine gene list files from all species
all_gene_lists <- c(unname(ath_gene_lists), unname(vvi_gene_lists))
families <- blast_to_families("all_vs_all.blast",
                              output_file  = "families.txt",
                              gene_list_files = all_gene_lists)

write_iadhore_config(
  genomes     = list(ath = ath_gene_lists, vvi = vvi_gene_lists),
  blast_table = "families.txt",
  table_type  = "family",
  output_path = "output/",
  file        = "config.ini"
)
}
}
