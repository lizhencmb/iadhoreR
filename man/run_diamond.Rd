% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diamond.R
\name{run_diamond}
\alias{run_diamond}
\title{Run an all-vs-all DIAMOND blastp search}
\usage{
run_diamond(
  fasta_file,
  output_file,
  work_dir = NULL,
  evalue = 1e-05,
  threads = 1,
  more_sensitive = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{fasta_file}{Path to a protein FASTA file, or a character vector of
paths (one per species) for multi-species searches}

\item{output_file}{Path to write the BLAST tabular results (outfmt 6)}

\item{work_dir}{Directory for the intermediate database files. Defaults to
the same directory as `output_file`}

\item{evalue}{E-value threshold for reporting hits. Default: 1e-5}

\item{threads}{Number of CPU threads. Default: 1}

\item{more_sensitive}{Use `--more-sensitive` mode. Default: FALSE}

\item{verbose}{Print DIAMOND output. Default: TRUE}
}
\value{
Invisibly returns the path to the output file
}
\description{
Builds a DIAMOND database from one or more protein FASTA files and runs an
all-vs-all blastp search. Output is in BLAST tabular format (outfmt 6),
ready for [blast_to_families()] or [parse_blast()].
}
\details{
When multiple FASTA files are supplied (e.g. one per species), they are
concatenated before building the database so that cross-species hits are
included in the output.
}
\examples{
\dontrun{
# Single species
fasta <- system.file("extdata", "proteome.all_transcripts.vvi.fasta",
                     package = "iadhoreR")
run_diamond(fasta, output_file = "vvi.blast")

# Multiple species (all-vs-all across species)
fastas <- c(
  system.file("extdata", "proteome.spp1.fasta", package = "iadhoreR"),
  system.file("extdata", "proteome.spp2.fasta", package = "iadhoreR")
)
run_diamond(fastas, output_file = "all_vs_all.blast")
}
}
